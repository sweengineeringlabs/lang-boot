[package]
name = "dev-engineeringlabs-rustboot-validation"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Type-safe validation framework with fluent builders"
keywords = ["validation", "fluent", "builder"]
categories = ["data-structures"]

[dependencies]
rustboot-macros = { path = "../rustboot-macros" }
regex = { version = "1", optional = true }


[dev-dependencies]
proptest = "1.5"

[features]
# Enable regex-based validation
regex = ["dep:regex"]
